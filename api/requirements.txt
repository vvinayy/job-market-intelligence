streamlit>=1.30
pandas>=2.0
plotly>=5.18
psycopg2-binary>=2.9
playwright==1.62.0   # pinned -- must match the cached browser in %LOCALAPPDATA%\ms-playwright (rev 1234); bumping this needs `python -m playwright install chromium` to succeed
fastapi>=0.110
uvicorn[standard]>=0.27
